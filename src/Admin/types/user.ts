export type User = {
    accessToken: string;
    roles: number;
    name: string;
    // email: string;
    // phone: string;
    // imgUrl: string;
    // isActive: boolean;
    // createAt: Date;
    // updateAt: Date
}